name: Mantener Backend Activo (Render)

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping al backend de Costeo DCM
        run: |
          echo "🔄 Verificando backend..."
          curl --fail --max-time 20 https://costosdcm-backend.onrender.com
