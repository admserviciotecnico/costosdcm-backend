name: Mantener Backend Activo (Render)

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping automático backend Costeo DCM
        run: |
          echo "Ping automático ejecutado en $(date)"
          curl -f https://costosdcm-backend.onrender.com || exit 1
