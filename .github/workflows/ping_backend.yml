name: Mantener Backend Activo (Render)
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
       - name: Enviar solicitud al backend
  run: |
    echo "Ping automático backend - $(date)"
    curl https://costosdcm-backend.onrender.com || exit 1

